<%= form_with(model: question, class: "contents") do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul class="list-disc ml-6">
        <% question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :question %>
    <%= form.textarea :question, rows: 4, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": question.errors[:question].none?, "border-red-400 focus:outline-red-600": question.errors[:question].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :asker %>
    <%= form.text_field :asker, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": question.errors[:asker].none?, "border-red-400 focus:outline-red-600": question.errors[:asker].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :answer %>
    <%= form.textarea :answer, rows: 4, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": question.errors[:answer].none?, "border-red-400 focus:outline-red-600": question.errors[:answer].any?}] %>
  </div>

  <div class="my-5 flex items-center gap-2">
    <%= form.label :answered %>
    <%= form.checkbox :answered, class: ["block shadow-sm rounded-md border order-first h-5 w-5", {"border-gray-400 focus:outline-blue-600": question.errors[:answered].none?, "border-red-400 focus:outline-red-600": question.errors[:answered].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
